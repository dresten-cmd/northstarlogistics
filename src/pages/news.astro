<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>[Appropriate Title – e.g. North Star Logistics – Virtual Trucking Company]</title>
  <link href="https://fonts.googleapis.com/css2?family=Viga&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/style.css"/>
</head>
<body>

<header>
  <a href="index.html" class="logo">North Star Logistics</a>
  <nav>
    <a href="index.html">Home</a>
    <a href="about.html">About</a>
    <a href="news.html" class="active">News</a>
    <a href="contact.html">Contact</a>
  </nav>
</header>

<section>
  <h2>Latest News</h2>
  <div style="max-width:900px; margin:0 auto;">
    <article style="margin-bottom:3rem; padding-bottom:2rem; border-bottom:1px solid #333;">
      <h3 style="color:var(--gold);">February 2026 Convoy Schedule Released</h3>
      <p><small>February 10, 2026</small></p>
      <p>Spring convoy season is here! Check Discord for the full lineup including Promods tours, coast-to-coast challenges, and special paint job reveals.</p>
    </article>

    <article style="margin-bottom:3rem; padding-bottom:2rem; border-bottom:1px solid #333;">
      <h3 style="color:var(--gold);">New Recruitment Requirements – 1.50 Update</h3>
      <p><small>January 28, 2026</small></p>
      <p>Following the latest ATS & ETS2 patches, we updated our minimum mod list and telemetry requirements. Full details in #announcements.</p>
    </article>

    <article>
      <h3 style="color:var(--gold);">Driver of the Year 2025 – Results</h3>
      <p><small>January 5, 2026</small></p>
      <p>Congratulations to SmokeyMcTruck – 2025 Driver of the Year! → Leaderboard</p>
    </article>
  </div>
</section>

<footer>
  <p><a href="index.html">North Star Logistics</a> – Virtual trucking done right.</p>
  <p class="copyright">© North Star Group 2026. All rights reserved.</p>
</footer>

</body>
</html>
---
import { getCollection } from 'astro:content';

// Get all news articles, sorted newest first
const allNews = await getCollection('news');
const sortedNews = allNews.sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---

<html lang="en">
  <head>
    <!-- Your meta, title, etc. -->
    <link href="https://fonts.googleapis.com/css2?family=Viga&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/style.css" />
    <title>News – North Star Logistics</title>
  </head>
  <body>
    <header>
      <!-- same header as other pages -->
      <a href="/" class="logo">North Star Logistics</a>
      <nav>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/news" class="active">News</a>
        <a href="/contact">Contact</a>
      </nav>
    </header>

    <section>
      <h2>Latest News</h2>
      {sortedNews.length === 0 && <p>No news yet. Check back soon!</p>}

      {sortedNews.map((post) => (
        <article style="margin-bottom: 3rem; padding-bottom: 2rem; border-bottom: 1px solid #333;">
          <h3 style="color: var(--gold);">{post.data.title}</h3>
          <p>
            <small>{new Date(post.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</small>
            • {post.data.author}
          </p>
          {post.data.image && <img src={post.data.image} alt="" style="max-width:100%; border-radius:8px;" />}
          <div set:html={post.body} />  <!-- Renders the Markdown body as HTML -->
        </article>
      ))}
    </section>

    <!-- footer -->
  </body>
</html>

<style is:global>
  /* Your CSS import or inline if needed */
</style>
